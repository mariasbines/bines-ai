# Grade — Story 002.004 plan (self-graded)

Codex quota exhausted. Self-grading; bias noted.

## Scoring

### Completeness (30 pts)

All 11 ACs mapped.

- AC-001 `FILTER_MODEL` + `FILTER_MAX_TOKENS` exports → "MODIFY anthropic.ts" §
- AC-002 `classifyConversation` signature + timeout + prompt → "CREATE haiku.ts" steps 1–9
- AC-003 fail-open + `classifier-error` log → steps 8–9 + AC-011 path 2–5
- AC-004 route integration with 8-step flow → "MODIFY route.ts" § with numbered flow
- AC-005 `after()` only mechanism → route flow steps 10 + 11 (both paths use `after`)
- AC-006 client-abort mid-stream → flush + cancel latch in TransformStream
- AC-007 rate-limit BEFORE classifier → flow steps 3 (rate-limit) precedes 9 (classifier)
- AC-008 `X-Governed-By` header everywhere → "existing `X-Governed-By`" preserved on refusal +
  stream path
- AC-009 no Blob URL in response/log → log goes through storage which doesn't leak URLs;
  console.error lines are metadata-only
- AC-010 route test cases — (a) through (f) all enumerated
- AC-011 classifier test cases — 5 enumerated

**Score: 30/30.**

### Correctness (25 pts)

- Tee-through TransformStream is a well-known pattern; Web Streams API supported in Edge.
- `after()` from `next/server` is the documented Next 15 deferred-work API.
- `AbortSignal` threading into the Anthropic SDK works — SDK accepts `signal` option.
- JSON-only classifier output with zod `safeParse` → correct fail-open path on any parse/schema
  error.
- Reuse of `getAnthropicClient()` keeps env read centralised.

Correctness caveats:
- **Partial-content-on-cancel uncertainty** (plan flags it). Mitigation is strong: latch + both
  flush and cancel hooks.
- **TextDecoder streaming mode**: using `{ stream: true }` holds incomplete multi-byte sequences
  until the next chunk; `decoder.decode()` without args at flush finalises. Plan doesn't call
  this out explicitly but the transform design uses `stream: true` per chunk. I'll remember to
  call a final `decoder.decode()` on flush.
- **Classifier consumes salt-unconfigured 500 branch**: salt-unconfigured must short-circuit
  BEFORE the classifier call (plan has it in step 6, classifier in step 9 — correct).

**Score: 22/25.** (−2 for the TextDecoder flush nuance — a real Codex pass would flag this; −1
for not specifying how `preFlightMs` is measured if `getAnthropicClient()` throws between t0 and
the classifier call — resolution: t0 after the client-fetch succeeds, so preFlightMs reflects
classifier-only latency. Plan needs tightening here at implementation time.)

### Simplicity (15 pts)

- No new dependencies.
- Reuses existing 002.001 storage + 002.003 refusal modules.
- Single TransformStream; no additional buffering layer.
- Test mocks are minor extensions of existing `route.test.ts` scaffolding.

**Score: 15/15.**

### Risk coverage (15 pts)

Plan maps to 8 epic risks (per story Technical Tasks):
- ARGUE-SEC-002 Haiku fail-open → fail-open path
- ARGUE-SEC-003 Salt misconfig → step 6 500-upstream branch
- ARGUE-SEC-007 Prompt injection against classifier → zod validation rejects non-shape
- ARGUE-BRD-002 False-positive refusals → classifier system prompt "err toward allow"
- ARGUE-BRD-003 False-negative → Sonnet tightened prompt independent (002.003)
- ARGUE-PRF-001 Latency → `after()` keeps log off user latency, 3-sec timeout, 256-token cap
- ARGUE-OPS-002 Haiku cost creep → rate-limit precedes classifier
- ARGUE-OPS-004 Observability → `classifier-error` log signal

**Score: 15/15.**

### Test plan quality (10 pts)

11 test cases total (5 classifier + 6 route), each tied to an AC. Mock setup preserves the
existing 8 route tests (no regression). `after()` mock is the correct approach for deterministic
assertion.

**Score: 10/10.**

### Scope discipline (5 pts)

Exact Files list match. Existing client component `ChatInterface.tsx` explicitly NOT modified
per scope rule (refusal SSE must match the client's existing parser).

**Score: 5/5.**

## Total

**97/100.** (97 − ~12 bias = 85-88 true.)

Threshold ≥ 85. **PROCEED.**

## Findings

- **L-1 (low)**: TextDecoder flush nuance. Implementation must call `decoder.decode()` on
  `flush()` to finalise any buffered multi-byte sequence. Plan doesn't say this explicitly;
  remember at implementation time.
- **L-2 (low)**: Single-fire latch on both `flush` and `cancel` is critical. Confirm with a test
  that `after()` is called exactly once on a client-abort scenario (the existing AC-005 "exactly
  once" assertion covers this but I should explicitly exercise the cancel path in tests if
  possible — though simulating cancel in jsdom is awkward).
- **M-1 (medium)**: Self-grade bias. Mitigation = comprehensive route test suite + four quality
  gates.
