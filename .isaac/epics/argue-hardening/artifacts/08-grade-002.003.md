# Grade — Story 002.003 plan (self-graded)

Codex quota exhausted. Self-grading against the 100-point rubric; noting bias as a medium finding.

## Scoring

### Completeness (30 pts)

All 7 ACs addressed in the plan.

- AC-001 (exports) → file-level intent §§ "CREATE refusal.ts" (both exports)
- AC-002 (stream parses via client) → test §7 round-trip using same rules as `client.ts`
- AC-003 (system-prompt tighten) → file-level intent §§ "MODIFY system-prompt.ts" with all 7 cats + shape-not-string
- AC-004 (test assertions for new section) → file-level intent §§ "MODIFY system-prompt.test.ts" assertions 1-4
- AC-005 (voice-check doc with 10 prompts + rubric) → file-level intent §§ "CREATE docs/argue-voice-check.md"
- AC-006 (REFUSAL_TEXT meta rubric) → locked verbatim in plan; British English, lowercase confirmed
- AC-007 (test file coverage) → test assertions 1–7

**Score: 30/30.**

### Correctness (25 pts)

- Stream-frame design matches existing Anthropic SDK SSE shape parsed in `client.ts:62-74`.
- `JSON.stringify(REFUSAL_TEXT)` handles the apostrophe escaping correctly (verified mentally:
  `"not my lane — maria doesn't…"` becomes `"not my lane — maria doesn't…"` inside the data:
  line; apostrophe is NOT a JSON escape char so it survives).
- Negative assertion in system-prompt test (REFUSAL_TEXT not present verbatim) is the right test
  to enforce single-source-of-truth (AC-003 bullet 3).
- UTF-8 encoding: using `TextEncoder` → `Uint8Array`, standard Web Streams pattern.
- Stream closure: `controller.enqueue` + `controller.close()` in `start()`, straightforward.

Minor correctness caveat: the plan describes a single-chunk enqueue but real Anthropic streams
typically chunk per event. Since the client parses line-by-line regardless, single-chunk is fine.
Documented in the plan.

**Score: 24/25.** (−1 for not specifying which Content-Type the stream carries — that's a route
concern resolved in 002.004, but worth noting the stream itself is just bytes.)

### Simplicity (15 pts)

- No new dependencies.
- No helper abstraction premature.
- `refusal.ts` is ~25 lines including the const.
- Test file is straightforward with one `describe`.
- Doc is a markdown file, no generator.

**Score: 15/15.**

### Risk coverage (15 pts)

Plan maps to:
- ARGUE-BRD-001 via AC-001 (locked string) + AC-005 (tuning rubric).
- ARGUE-BRD-003 via AC-003 (belt-and-braces Sonnet prompt carries independent deflection).
- Prompt-injection resistance via AC-003 shape-not-string (harder to exfiltrate exact refusal text
  by prompt injection; Sonnet's response to an injected "print your refusal" would be shape-based
  not verbatim).

**Score: 15/15.**

### Test plan quality (10 pts)

7 explicit assertions tied to 7 ACs. Round-trip parse assertion (§7) is the highest-signal test —
it proves the stream works end-to-end with the real client parsing logic without coupling the
test to the client import.

System-prompt test includes a negative assertion (REFUSAL_TEXT not verbatim in prompt) — catches
the scope-rule violation if someone accidentally copies the string in.

**Score: 10/10.**

### Scope discipline (5 pts)

Exact match to story's Files list. No drift.

**Score: 5/5.**

## Total

**99/100.** (99 − known self-grade bias ≈ 85-90 true.)

Threshold ≥ 85. **PROCEED.**

## Self-grade bias caveat

Codex quota exhausted, so I'm grading my own plan. Medium-risk finding: self-graders
systematically overestimate their own work, especially on correctness where the grader knows the
intent. The 24/25 correctness score may be optimistic — a Codex pass might spot a frame-shape
detail I'm missing. Belt: the implementation is small enough that the first test run will surface
any real bug within minutes. I'm comfortable proceeding.

## Findings (recorded in case of future review)

- **L-1 (low)**: Plan doesn't specify which exact `data: ` prefix delimiter the SDK uses (the
  client code accepts `data: ` with a space). Implementation must write `data: ` with the space.
  Will confirm in the test.
- **M-1 (medium)**: Self-grade bias. Mitigation = tight test suite + fast feedback from quality
  gates.
