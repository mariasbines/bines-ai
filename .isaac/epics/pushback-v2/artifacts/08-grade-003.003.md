# Self-Grade: 003.003 — argue-judge core module

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.003.md`.
**Pass threshold:** 85 / 100.
**Result:** **89 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (19 / 20)

All 12 ACs mapped to numbered implementation steps with file paths and exports. Architecture-locked text (schema, prompt) is reproduced verbatim in the plan §Design 2 + §Design 4 — not paraphrased. Every module has its own §Design subsection with full source samples. Six modules + their tests, each enumerated.

The plan covers:
- Schema with `EXCERPT_MAX_CHARS` exported as single source of truth (§Design 2).
- Storage mirroring `argue-log` with the +1 helper `findVerdictByConversationId` (§Design 3).
- Prompt with canonical anti-injection sentence + transcript fence + escape function (§Design 4).
- Runner with fail-shut error policy + AbortController timeout (§Design 5).
- Loader with filter + dedupe + sort + null-on-error (§Design 6).
- TDD discipline ordered (§Design 8).
- All eight risk mitigations enumerated.

**Penalty (-1)**: the plan does not enumerate the exact full set of `validVerdict()` and `makeVerdict()` fixture builder fields. The discriminated-union-light shape is shallow (no nested unions like Fieldwork has), so this is a small construction-cost item only — trivial to write during execute.

**Score: 19/20.**

### 2. AC ↔ code traceability (19 / 20)

Verification matrix maps all 12 ACs to plan steps + test specs. File paths and function names explicit. Architecture references are file:line-precise (`03-architecture.md:42-55` for schema; `:117-162` for prompt; `:166-173` for transcript builder).

**Penalty (-1)**: AC-007's "fail-shut" property is described in the plan but the runner test for it is implicit (i.e., "no spec asserts a default-verdict return"). A dedicated negative spec (`expect(judgeConversation(...))` resolved value pattern is intentionally absent) is documented but could be sharper as a comment in the test file. Cosmetic.

**Score: 19/20.**

### 3. Test coverage plan (17 / 20)

Spec count target ~50 across 5 files. Edge cases enumerated for each module. Coverage target ≥85% with reasoning ("trivially exceeded" given small surface).

**Penalty (-1)**: the runner timeout test uses `vi.useFakeTimers()` + manual `advanceTimersByTimeAsync(8001)`. This pattern is correct but introduces a per-test wrapping concern (existing tests in this codebase don't use fake timers, so beforeEach/afterEach hygiene needs care to not leak). Plan flags the approach but doesn't lock the cleanup pattern.

**Penalty (-1)**: no explicit test for `findVerdictByConversationId` returning the LATEST when both today and yesterday have a match. The plan covers "cross-day match" but should sharpen into "two matches → returns the one with later judged_at".

**Penalty (-1)**: no explicit test for `readArgueJudgeDay`'s behaviour on a malformed JSONL line. The argue-log analogue tests this; the mirror-line-by-line plan implies coverage but doesn't list the spec. Add during execute.

**Score: 17/20.**

### 4. Risk mitigation (18 / 20)

All eight risks from `04-assessment.md` mapped to specific plan steps + test specs. Each has a concrete code action.

**Penalty (-1)**: PB2-BRD-001 (judge false-positive — clean conversations flagged harmful) mitigation is "tune via admin review during soft-launch". This is honest and architecture-accurate, but the in-flight test plan doesn't include a "voice-check fixture" spec — e.g., "a clearly-on-brand exchange with no harm content does NOT trigger `harm_in_visitor_messages: true` in the prompt's expected output". Hard to test deterministically without running Sonnet, but a fixture-shape spec ("the prompt asks for harm classification only on the five named categories") would be nice. Defensible to defer.

**Penalty (-1)**: PB2-PRF-001 (build-time enrichment cost) is "deferred". Plan doesn't include a perf-budget spec (e.g., "loader runs in <X ms with a 90-day fixture"). Architecture-aligned (deferred), but a timing assertion would tighten.

**Score: 18/20.**

### 5. Scope discipline (16 / 20)

Files list matches story exactly (10 files: 5 source + 5 test). All MODIFY scope-rules from the story are documented as out-of-scope in §Design 9.

**Penalty (-2)**: the plan introduces `EXCERPT_MAX_CHARS = 240` as a top-level export not explicitly in the story's AC-001 list (the AC says "Also exports a top-level constant `EXCERPT_MAX_CHARS = 240`"). Wait — re-reading: the AC DOES specify this. So this is in-scope. **Score correction: no penalty.** Adjusting:

**Penalty (-2)**: the plan introduces `JUDGE_MAX_TOKENS = 512` and `JUDGE_TIMEOUT_MS = 8000` as runner module-level constants. Architecture line 198 specifies these values; not story ACs explicitly. They're implementation details, not exports. Defensible scope, but a stricter reading would put them in a shared `constants.ts`. Single point off for not flagging this consideration.

**Penalty (-2)**: the plan reuses `dayKeyUtc` + `isValidDayKey` from `argue-log/day.ts` per architecture lock. This is a cross-package import that the plan handles correctly. No issue here. **Score correction: no penalty for this.**

**Net penalties on scope**: -4 for general scope-discipline tightness on cross-module imports + constants. Could be cleaner but justifiable.

**Score: 16/20.**

---

## Total: 19 + 19 + 17 + 18 + 16 = **89 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute (not blocking grade)

1. **Add a `findVerdictByConversationId` "latest wins across today + yesterday" spec** — explicit two-match cross-day fixture.

2. **Add a `readArgueJudgeDay` malformed-JSONL-line spec** — mirror argue-log's pattern.

3. **Add a runner test that asserts `judge_model` is set to `DEFAULT_MODEL` and `schema_version: 1`** — server-controlled fields aren't overrideable by the LLM.

4. **Add a test that proves the LLM's attempt to override `conversation_id` is dropped** — the runner's spread order ensures server values win, but a regression spec belts that.

5. **Fake-timers cleanup**: `afterEach(() => vi.useRealTimers())` in `runner.test.ts` to avoid leaking fake-timer state.

6. **Coverage check**: after tests pass, run `pnpm test --coverage` (if configured) on the new module; if not, eyeball the spec → file mapping for ≥85%.

None of these affect the grade. All five are tightening adds during execute.

---

## Confidence

High. The architecture-locked text removes the largest source of uncertainty (voice + JSON shape). The mirror-argue-log pattern de-risks storage. The runner is the riskiest novel surface (AbortController + Anthropic SDK signal threading) but the SDK pattern is established by the existing chat route's stream call. Loader is a pure data-pipeline function — easiest to test.

Proceeding to execute.
