# Self-Grade: 003.001 — Schema diff + conversation_id threading

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.001.md`.
**Pass threshold:** 85 / 100.
**Result:** **94 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (20 / 20)

Every AC (AC-001 through AC-011) has a numbered implementation step, a code/file reference, and a test reference. Every file in the story's "Files" list appears in the plan's Step list. Two new test files are explicitly enumerated (`id.test.ts`, plus extensions to four existing test files). The new file (`src/lib/conversation/id.ts`) has its full source written in §Step 3 — not just described.

The plan covers:
- Schema edit + back-compat fixture (Steps 1, 2)
- New `newConversationId()` module + tests (Steps 3, 4)
- Validate schema extension + result-type extension + four new specs (Steps 5, 6)
- Client options extension + body construction discipline + three new specs (Steps 7, 8)
- Component lazy-mint via `useRef`, `from_slug: null` placeholder, four new specs (Steps 9, 10)
- Route fallback mint, threading into both easter-egg and streaming entry constructions, six new specs (Steps 11, 12)
- Local quality-gate run + commit + push (Steps 13, 14)

No AC is left to "self-evidently met by the code". Every AC has at least one explicit test spec listed.

**Score: 20/20.** No partial — every AC has a step + a test.

### 2. AC ↔ code traceability (20 / 20)

The "AC Verification Matrix" maps every AC to the responsible plan step(s) AND to specific test(s). File paths are explicit (`src/lib/conversation/id.ts`, `src/lib/argue-log/__tests__/schema.test.ts`, etc.). Function and field names are explicit (`newConversationId()`, `conversationIdRef`, `CHAT_REQUEST.conversation_id`, `ARGUE_LOG_ENTRY.from_slug`). Test spec names are direct quotations of intent (e.g. "round-trips a pre-Phase-A entry shape (no conversation_id / from_slug)").

The plan even calls out where the existing tests will continue to pass without modification (Step 12 explanatory paragraph) — that's traceability of the back-compat property, not just the forward property.

**Score: 20/20.**

### 3. Test coverage plan (19 / 20)

Plan enumerates ~22-25 new specs across 5 files. Edge cases identified:

- empty conversation (no submit) → no mint
- legacy body (no fields) → server fallback mints
- valid body with both fields → threaded verbatim
- `from_slug: null` (explicit) vs omitted → both tolerated
- malformed UUID at wire → 400
- multiple sequential turns share id
- easter-egg path also gets fields

Coverage target stated (≥80% on new module, no regression on modified files). Per-file coverage of the new module (`src/lib/conversation/`) is trivially over the threshold given the tiny surface (one function, three test paths).

**Minor gap (-1):** the plan does not enumerate an explicit Vitest coverage-collection command nor confirm the project's `vitest.config.ts` runs coverage by default. The QUALITY_GATES.md doc says "≥80% lines/functions/branches" but the project's `pnpm test` is `vitest run --passWithNoTests` (no `--coverage` flag). I'll have execute step note coverage informally if the project doesn't auto-collect — for a one-function module, manual visual inspection is defensible.

**Score: 19/20.**

### 4. Risk mitigation (19 / 20)

All three risks tied to this story in `04-assessment.md` are addressed:

- **PB2-SEC-008 conversation_id collision/guessability** — three-layer defence: `crypto.randomUUID()` mint (UUID v4, ≥122 random bits), zod `.uuid()` schema gate, route 400 on malformed. Test: regex assertion of v4 shape + 400-on-non-UUID test. Strong.
- **PB2-DAT-001 schema additive drift** — `schema_version: 1` literal preserved, two back-compat fixtures (pre-shape + new-shape round-trip), three negative fixtures (malformed UUID, number from_slug, etc.). Strong.
- **PB2-PRV-003 from_slug cross-correlation in argue-log** — accepted at admin-only-access boundary; the plan documents that no public surface exposes from_slug in this story. Defensible.

**Minor gap (-1):** the rollback section discusses zod's strip-extra behaviour. Worth verifying that `ARGUE_LOG_ENTRY` is not in `.strict()` mode (the default is non-strict; the existing schema definition above does not call `.strict()` — confirmed). No code action needed; this is verified by reading the schema in Step 1 — but the plan could have made the mode explicit. Cosmetic.

**Score: 19/20.**

### 5. Scope discipline (16 / 20)

Plan's "Files" list matches the story's "Files" list exactly:

CREATE: `src/lib/conversation/id.ts`, `src/lib/conversation/__tests__/id.test.ts` ✓
MODIFY: `src/lib/argue-log/schema.ts`, `src/lib/argue-log/__tests__/schema.test.ts`, `src/lib/chat/validate.ts`, `src/lib/chat/__tests__/validate.test.ts`, `src/lib/chat/client.ts`, `src/lib/chat/__tests__/client.test.ts`, `src/components/ChatInterface.tsx`, `src/components/__tests__/ChatInterface.test.tsx`, `src/app/api/chat/route.ts`, `src/app/api/chat/__tests__/route.test.ts` ✓
DELETE: none ✓

The plan explicitly enumerates "out of scope" in §Design 9 — `?from=` capture, chat-end beacon, preface, judge module, fieldwork components, push-back deletion, schema_version bump. All seven scope-out items match the story's "Scope rules".

**Penalty (-2):** the test file extension to `<ChatInterface>` introduces `userEvent.setup()`. The plan correctly checks the package.json for `@testing-library/user-event` as a transitive dep, but does not verify it definitively. If the dep is missing, the plan notes the `fireEvent.change` + `fireEvent.click` fallback — this is a defensive "or" rather than a single locked path. Slight scope-discipline cost: I should have grepped the lock file or `package.json` for `user-event` before writing the plan. Quick check during execute; if missing, fall back without changing scope.

**Penalty (-2):** the plan introduces `validateRequest` returning two new optional fields on the `ok=true` branch. This is a strictly-additive type change but it does propagate beyond the body-validation surface — any existing caller that destructures the result keeps working (TS does not narrow to disallow extras), but a future reader could be confused about whether the new fields are mandatory. Mitigation: the plan documents the additivity explicitly and the test extension covers all four cases. Could be sharper — e.g., a JSDoc comment on `ValidateResult` calling out the Phase A additions explicitly.

**Score: 16/20.**

---

## Total: 20 + 20 + 19 + 19 + 16 = **94 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute (not blocking grade)

1. **Verify `@testing-library/user-event` is a dep before relying on `userEvent.setup()`.** If absent, fall back to `fireEvent.change` + `fireEvent.click` (already documented in plan §Step 10). This is a 30-second `grep user-event package.json` check at the start of execute.

2. **Add a brief JSDoc comment on `ValidateResult.ok=true`** noting the Phase A additions (`conversation_id?`, `from_slug?`). One-line "Phase A — story 003.001" comment is enough to make the additivity explicit for future readers.

3. **Confirm `ARGUE_LOG_ENTRY` is not `.strict()`** before proceeding (the rollback's "unknown fields silently dropped on revert" claim depends on it). 5-second visual check; the existing definition is plain `z.object(...)` so this is fine.

4. **`pnpm test --run` flag** — the story uses `pnpm test --run` in AC-011 / Step 13; the project's package.json defines `"test": "vitest run --passWithNoTests"` so `pnpm test --run` is equivalent (the `run` flag is doubled but harmless). Use either form consistently.

None of these affect the grade. All four are in-flight cleanups during execute.

---

## Confidence

High. Plan is concrete, every AC traces to file:line, every risk has a test. The two penalty deductions (scope-discipline -4) are the honest assessment of "could be tighter on dep verification + JSDoc"; not blockers, not regressions on the story spec.

Proceeding to execute.
