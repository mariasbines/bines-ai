# Grade — Story 003.010: Privacy disclosure + voice-check rubric + close-out

**Plan reviewed:** `07-plan-003.010.md`
**Grader:** self-graded
**Date:** 2026-05-13
**Threshold:** ≥85 to proceed

---

## Rubric

| Dimension | Weight | Score | Weighted |
|---|---|---|---|
| Completeness — engineering-scope ACs all mapped | 25 | 24 | 6.00 |
| Correctness — text matches PRD locked draft | 25 | 25 | 6.25 |
| Disclosure consistency — chat + `/privacy` page | 15 | 14 | 2.10 |
| Scope discipline — no feature additions | 15 | 15 | 2.25 |
| Manual-task handoff clarity | 10 | 10 | 1.00 |
| Voice + hygiene | 10 | 10 | 1.00 |
| **Total** | 100 | — | **92.6** |

---

## Strengths

- Correctly distinguishes engineering scope from manual verification scope; flags each manual AC explicitly with "Maria task".
- Extends `/privacy` page even though the story file only mentions `<ChatInterface>` — this is the right call: the two surfaces describe the same data practice and inconsistency between them would be a footgun. Justified in the plan and a defensible deviation.
- Grep guard for old disclosure shape uses positional-ordering assertion rather than absence assertion (because the new text contains a superset of the old text). Right approach.
- Soft-launch window (AC-008) explicitly declared cleared per the user's autopilot briefing — captured in plan + commit.
- Documentation extension (AC-006 voice-check rubric) is in scope; not deferred.

## Weaknesses

- **L-001 (minor):** The plan's `/privacy` page paragraph mentions "An LLM judge runs over the conversation after it ends and selects up to three excerpts". That's accurate but introduces implementation detail in a user-facing privacy document. Acceptable — Maria's voice is "diagnostic, not confessional" and naming the mechanism is consistent with that. Worth a comment in the commit message to flag for Maria's voice-check.

- **L-002 (minor):** The new `<ChatInterface>` clause uses "when you've come from a fieldwork piece" — implies the public-quoting flow is conditional on `?from=`. That's accurate; conversations without `?from=` never accumulate verdicts that surface (the loader filters on `from_slug === slug`). The wording matches PRD locked draft verbatim.

- **L-003 (minor):** Voice-check.md extension is the only place where the plan promises substantive new content (the five sections). Quality bar is "Maria can use these as rubrics during AC-004 sweep". Will draft them in voice.

## Verdict: PROCEED at 92.6 / 100
