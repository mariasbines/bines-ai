# Self-Grade: 003.002 — `?from=<slug>` capture + chat-route preface

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.002.md`.
**Pass threshold:** 85 / 100.
**Result:** **91 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (19 / 20)

Every AC (AC-001 through AC-010) has a numbered implementation step + test reference. Plan correctly identifies the runtime mismatch (Edge vs `node:fs`) and resolves it before writing code — not as a post-hoc fix. Sample `buildPiecePreface` source written inline in §Design 4. Test mocking patterns explicit for both `useSearchParams` (`next/navigation`) and `getFieldworkBySlug` (`@/lib/content/fieldwork`).

The plan covers:
- Runtime switch + rationale (§Design 1)
- Sticky capture pattern via lazy initial state (§Design 2)
- Suspense boundary check at `/argue/page.tsx` (Step 1)
- Locked preface text matching architecture (§Design 4)
- Empty-string normalisation in two places (§Design 7)
- Route preface composition with try/catch defence (§Design 5)
- All six AC-009 route-test cases (Step 7)

**Penalty (-1)**: the plan does not specify the exact list of fields needed in the test fixture builder for `Fieldwork`. The schema is a discriminated union — building a valid `'in-rotation'` shape requires `id, slug, title, published, tags, media, pushback, excerpt`. The plan sketches but doesn't enumerate. Easy to fix during execute by reading `src/lib/content/types.ts:60-78`. Cosmetic.

**Score: 19/20.**

### 2. AC ↔ code traceability (19 / 20)

AC Verification Matrix maps all 10 ACs to plan steps and tests. File paths and function names are explicit. The architecture-locked preface text is referenced by file:line (`03-architecture.md:248-253`).

**Penalty (-1)**: AC-008 ("visitor-controlled slug never reaches the prompt as interpolation source") maps to "Step 6 + verified by `getFieldworkBySlug` design", which is partially structural rather than directly testable. The negative assertion in route-test (d) is the closest mechanical proof, but a dedicated grep-style test ("system prompt does not contain the literal slug string") would tighten this. The plan acknowledges it via the negative assertion approach but doesn't ship a dedicated AC-008 spec. Defensible but trims a point.

**Score: 19/20.**

### 3. Test coverage plan (18 / 20)

Plan enumerates ~13-15 new specs. Edge cases identified: no param, empty param, valid slug, unknown slug, path-traversal slug, sticky across rerender, preface text fixtures, log-entry attribution preservation.

**Penalty (-1)**: no explicit test for the `try/catch` defensive path in route.ts (a `getFieldworkBySlug` that throws). The plan mentions the catch is for "regression in another MDX file" — this is reachable in tests by `mockGetFieldworkBySlug.mockRejectedValueOnce(...)`. Adding a single spec covering the throw path would round out coverage. Note in execute as a minor add.

**Penalty (-1)**: coverage on `buildPiecePreface` is asserted as "trivially exceeded" but the function has four branches (excerpt present, excerpt empty-string, body fallback, edge case where body is also empty). The fallback path is only loosely tested. Defensible but points trim.

**Score: 18/20.**

### 4. Risk mitigation (19 / 20)

All three story-mapped risks addressed with concrete mitigations:

- **PB2-SEC-003** (slug injection) → four-layer defence: lookup-only, exact-match design, no interpolation, regression fixture for path-traversal. Strong.
- **PB2-BRD-001** (voice via preface) → architecture-locked text + lowercase + canonical-phrase assertions. Strong.
- **PB2-BRD-002** (Maria sign-off) → text matches architecture lock; deferred to dev-branch review per autopilot instructions; commit message records the deferral. Acceptable given Maria-out constraint.

**Penalty (-1)**: PB2-BRD-002 mitigation is the weakest of the three — "deferred sign-off" is honest but pushes the risk to a later cadence rather than closing it inline. The plan could have proposed a fallback (e.g., a feature flag that gates the preface; off until Maria signs off). Given the constraint that this is dev-only and Maria reviews dev separately, the deferred approach is defensible but a flag would be tighter. Single point off.

**Score: 19/20.**

### 5. Scope discipline (16 / 20)

Plan's "Files" list matches the story's "Files" list exactly:

MODIFY: `src/components/ChatInterface.tsx`, `src/components/__tests__/ChatInterface.test.tsx`, `src/lib/chat/system-prompt.ts`, `src/lib/chat/__tests__/system-prompt.test.ts`, `src/app/api/chat/route.ts`, `src/app/api/chat/__tests__/route.test.ts` ✓
CREATE: none ✓
DELETE: none ✓

Plus `src/app/argue/page.tsx` may need a one-line Suspense addition (Step 1). This is a justified scope addition (Next.js 15 compile-time requirement; the route would not build otherwise). Documented in the plan and in commit-message scope notes.

**Penalty (-2)**: the runtime switch `edge` → `nodejs` is a meaningful architectural decision. While necessary (the architecture's `getFieldworkBySlug` call from chat is incompatible with Edge), it deserves a separate flag or follow-up note that this is a clarifying decision. The plan documents it well in §Design 1 — but it's still scope expansion beyond the story's stated wire-shape change. Acceptable given the architecture forced the choice; not a clean "implement what was specified".

**Penalty (-2)**: the empty-string normalisation `from_slug ?? null` → `from_slug || null` in the route is a story-003.001 file edit propagated into 003.002. This is a small surgical change to the story 003.001 implementation in service of AC-009 (e), but technically modifies a line that 003.001 already shipped. Plan documents it in §Design 7 as defence-in-depth — acceptable but slight scope-coupling cost.

**Score: 16/20.**

---

## Total: 19 + 19 + 18 + 19 + 16 = **91 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute (not blocking grade)

1. **Read `/argue/page.tsx` first** — confirm Suspense boundary status. If missing, add it as part of this story (Step 1, one-line wrap).

2. **Add a fixture builder helper in `system-prompt.test.ts`** — produces a valid `Fieldwork` for `buildPiecePreface` tests. Required because the discriminated union on `status` makes inline literal fixtures verbose.

3. **Add one extra route-test spec** for the `getFieldworkBySlug` throw path — rounds out the try/catch defensive branch coverage. ~5 lines.

4. **AC-008 negative assertion**: in route test (d) for the path-traversal case, also assert the system prompt does NOT contain the literal substring `'../../etc/passwd'`. Belt-and-braces; takes one line.

5. **Voice-check note in commit message**: explicitly state the preface text matches `03-architecture.md:248-253` verbatim and that Maria's per-story sign-off is deferred to her dev-branch review per the autopilot run constraints. This is the canonical record of the deferred AC.

None of these affect the grade. All five are easy adds during execute.

---

## Confidence

Medium-high. The runtime switch is the highest-novelty piece — well-documented but novel for this codebase. Build will surface any Suspense or runtime issues immediately. Mocking `next/navigation` is net-new but trivially safe.

Proceeding to execute.
