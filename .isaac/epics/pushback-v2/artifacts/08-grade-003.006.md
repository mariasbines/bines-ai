# Self-Grade: 003.006 — `/api/argue-judge/sweep` cron + `vercel.json`

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.006.md`.
**Pass threshold:** 85 / 100.
**Result:** **90 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (19 / 20)

All 8 ACs mapped to numbered steps + tests. Cleanup-route auth pattern reused exactly. Selection logic (group → skip pre-Phase-A → skip already-judged → run + write per-conversation) articulated step-by-step. `?day=` override + validation explicit. Per-conversation isolation in `try/catch` with continue.

The plan covers:
- Auth via mirrored `timingSafeEqual` (Design §1).
- Target-day resolution (Design §2).
- Three-way selection (skip pre-Phase-A, skip already-judged, run rest) with code sample (Design §3).
- Response envelope shape (Design §4).
- `vercel.json` exact target shape (Design §5).
- Ops doc 5-section structure (Design §6).

**Penalty (-1)**: the plan documents reading both target day + today's verdicts to build `judgedIds` (cross-day verdict day-key handling), but doesn't enumerate the test that exercises this specifically. Could be tightened.

**Score: 19/20.**

### 2. AC ↔ code traceability (19 / 20)

Verification matrix maps all 8 ACs. AC-006's `vercel.json` AC is mapped to "implicit via build success" — defensible since the file is JSON and a malformed schema would fail at deploy, but a unit-test that parses the file and asserts the cron entries would tighten this. The orchestrator pattern in story 002.001 of argue-hardening did exactly this; consistent with project precedent would mean adding it.

**Penalty (-1)**: AC-006 trace could be a literal `JSON.parse(readFileSync('vercel.json'))` assertion as its own test.

**Score: 19/20.**

### 3. Test coverage plan (18 / 20)

13-15 specs across the new test file. Edge cases identified: empty day, all-already-judged, mixed, isolation on error, pre-Phase-A skip, day override, malformed override.

**Penalty (-1)**: no spec for "two consecutive errors don't double-count" — i.e., `errors === 2` after two failures. Trivially covered by extending one of the planned specs.

**Penalty (-1)**: no spec for the `day` field in the response envelope matching the actual targetDay (default vs override).

**Score: 18/20.**

### 4. Risk mitigation (19 / 20)

All four risks mapped:
- PB2-SEC-005: timing-safe compare + `?day=` regex validation + bounded blast radius via daily rate cap.
- PB2-OPS-001: manual `?day=` replay + ops doc + monthly check.
- PB2-OPS-002: bounded by chat-route rate limit; cost documented.
- PB2-PRF-002: `maxDuration: 300` covers v1 scale.

**Penalty (-1)**: PB2-SEC-005 mitigation could include a regression spec for "?day= cannot be a path traversal shape" — `?day=../../etc/passwd` should 400. The plan implies coverage via the regex check but doesn't list it as a spec.

**Score: 19/20.**

### 5. Scope discipline (15 / 20)

Files list matches story exactly: 3 created (route + tests + ops doc), 1 modified (`vercel.json`).

**Penalty (-2)**: the plan introduces a defensive `convEntries[0]?.from_slug ?? null` pattern even though the group construction guarantees ≥1 entry. Defensible defence-in-depth, but a small scope creep over the architecture-locked "first entry" rule.

**Penalty (-3)**: ops doc has five sections, all per AC-007. The sections cover what's asked but the plan section §6 introduces some judgment calls ("ordering matters: cleanup deletes argue-log entries older than 90 days BEFORE sweep runs") that could be considered out-of-AC commentary. Minor — the rationale is sound, but tighter scope would skip such notes.

**Score: 15/20.**

---

## Total: 19 + 19 + 18 + 19 + 15 = **90 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute

1. **Add a `vercel.json` integrity test.** `JSON.parse(readFileSync('vercel.json'))` and assert both cron entries + the `functions.maxDuration` shape. Single spec.

2. **Add a `errors === 2` regression spec.** Two consecutive failures should both be counted.

3. **Add a `day` field response assertion.** Both default-yesterday and explicit-override paths should include `day` in the envelope matching the resolved targetDay.

4. **Add a `?day=../../etc/passwd` 400 test.** Tightens PB2-SEC-005 mitigation.

5. **Voice-check the `docs/argue-judge-ops.md`** against `docs/argue-log-ops.md` style. British English. Lowercase headers.

None of these affect the grade.

---

## Confidence

High. The route is the third edge route in this codebase (cleanup, run, sweep) — the patterns are well-established. The vercel.json modification is the smallest possible diff. The ops doc is documentation-as-code.

Proceeding to execute.
