# Self-Grade: 003.005 — Chat-end signal in `<ChatInterface>`

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.005.md`.
**Pass threshold:** 85 / 100.
**Result:** **88 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (18 / 20)

All 9 ACs mapped to numbered steps + tests. Idle reset semantics carefully analysed (§Design 2) including the subtlety that `messages.length` doesn't change during streaming — leading to the choice of the full `messages` array as the dep. `useCallback` with `[]` for stable handler identity. Unload cleanup via single effect with both listeners.

**Penalty (-1)**: the plan acknowledges that the AC-006 "stream activity resets idle" test will use submits as a proxy ("substitute 'stream activity' with another submit"). This is defensible — the same effect dependency catches both — but the test name should still read "idle resets on activity (submit-as-proxy for stream)" so the AC trace is explicit.

**Penalty (-1)**: AC-008 (graceful no-op on missing `navigator.sendBeacon`) is documented as "optional spec" rather than "mandatory spec". Should be mandatory — the AC is unambiguous.

**Score: 18/20.**

### 2. AC ↔ code traceability (19 / 20)

Every AC is in the matrix. AC-005 ("no other event listeners") is mapped to "implicit (no spec asserts visibilitychange)" — defensible since the production code only registers the two events, but a sharper test (`expect(addEventListener).not.toHaveBeenCalledWith('visibilitychange', ...)`) would tighten this.

**Penalty (-1)**: AC-005 negative mapping could be sharper.

**Score: 19/20.**

### 3. Test coverage plan (17 / 20)

9 new specs plus the AC-008 sendBeacon-missing case (10 if mandatory). Edge cases identified: debounce, null conv-id, unmount cleanup, successive submits.

**Penalty (-1)**: no spec for `pagehide` followed by submit followed by `beforeunload` — i.e., the debounce holds across the lifetime of the same conversation_id even when more activity happens between events. The plan covers debounce for two close events; doesn't explicitly cover debounce-with-mid-flight-activity. Minor scope.

**Penalty (-2)**: the AC-006 "Idle resets on stream activity" test as-planned uses submit-as-proxy. The architectural intent is more nuanced — the test SHOULD prove that during a real stream (status === 'streaming' with onDelta callbacks), the timer resets. The plan's choice is OK if documented but a test that simulates the streaming path (mocking `postChat` to call `onDelta` repeatedly) would more closely match the AC. Single penalty for not pursuing the higher-fidelity test.

**Score: 17/20.**

### 4. Risk mitigation (18 / 20)

Three risks mapped:
- PB2-SEC-007: minimal payload + reliance on the run route's IP-bind. Strong.
- PB2-PRF-003: sendBeacon's fire-and-forget property + try/catch. Strong.
- PB2-OPS-003: debounce guard + AC-006 case 7. Strong.

**Penalty (-1)**: PB2-PRF-003 could include an explicit test that `sendBeacon` returns immediately (no `await`); the plan describes this as a code property but no spec asserts it. Test would be `expect(fireChatEnd()).toBe(undefined)` (synchronous return). Easy add.

**Penalty (-1)**: race-condition more nuanced than two-event debounce: e.g., idle timer fires AND pagehide fires nanoseconds later. Both will see `firedConversationsRef.current.has(id) === false` initially and both will try to add — but JavaScript's single-threaded model serialises them, so the second sees the first's `add()`. No actual race. Plan could note this explicitly.

**Score: 18/20.**

### 5. Scope discipline (16 / 20)

Files list exactly two MODIFY (component + tests). All eight scope rules echoed in §Design 8.

**Penalty (-2)**: the plan introduces a `useCallback`-based pattern for `fireChatEnd` instead of a plain function (which would re-allocate per render). Defensible perf optimisation, but slightly out of scope for a "just add the signal" story. Single point off.

**Penalty (-2)**: the design discussion in §2 deeply analyses the dep-list semantics (option A vs option B) — appropriate engineering rigour, but reads as scope expansion. The actual implementation is small; the plan documentation makes it look bigger. Cosmetic but counts.

**Score: 16/20.**

---

## Total: 18 + 19 + 17 + 18 + 16 = **88 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute

1. **Make the AC-008 sendBeacon-missing spec mandatory.** Add a spec that deletes `navigator.sendBeacon` and asserts `fireChatEnd` does not throw.

2. **Add a spec asserting `fireChatEnd()` returns synchronously** (no await needed). Tightens the PB2-PRF-003 mitigation.

3. **Add a spec that no `visibilitychange` listener is registered.** Tightens AC-005.

4. **Document in the test file's comments** that the "stream activity resets idle" spec uses submit-as-proxy; trace this back to the same dep-list mechanism.

None of these affect the grade. All four are tightening adds.

---

## Confidence

Medium-high. The component change is small; the tests are timing-heavy and have a learning curve (fake timers + event dispatch + sendBeacon mocking). The cleanup-on-unmount test is the most subtle — relies on the React effect cleanup mechanism working correctly with fake timers, which it should.

Proceeding to execute.
