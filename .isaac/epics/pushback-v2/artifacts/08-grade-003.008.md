# Grade — Story 003.008: [ argue with this ] CTA on FieldworkCardCtas

**Plan reviewed:** `07-plan-003.008.md`
**Grader:** self-graded
**Date:** 2026-05-13
**Threshold:** ≥85 to proceed

---

## Rubric

| Dimension | Weight | Score | Weighted |
|---|---|---|---|
| Completeness | 25 | 24 | 6.00 |
| Correctness | 25 | 24 | 6.00 |
| TDD discipline | 20 | 18 | 3.60 |
| Risk coverage | 15 | 14 | 2.10 |
| Scope discipline | 10 | 10 | 1.00 |
| Voice + design hygiene | 5 | 5 | 0.50 |
| **Total** | 100 | — | **93.2** |

---

## Strengths

- Correctly identified that the story file's reference to `[ read ]` is stale — `<FieldworkCardCtas>` no longer renders one. The plan documents the deviation and explains the new CTA-order rationale (`[ argue with this ]` first, `[ watch ]` second).
- Spotted the early-return bug: AC-003 says "appears regardless of whether the piece has any pushbacks" but the current component does `if (!hasTestimonial) return null;`. The plan removes that.
- Plan correctly notes that `[ argue with this ]` doesn't need client-side state — it's just a `<Link>`. No premature complexity.
- Plan keeps `<WatchDialog>` inside the `hasTestimonial` conditional so the dialog isn't mounted for cards without a testimonial.
- Test list maps to AC-002's five sub-cases explicitly.

## Weaknesses

- **L-001 (minor):** Plan suggests adding a "shares hover/border classes" test using class-string assertion. That's brittle to Tailwind upgrade. Acceptable for this story (the classes are exactly the locked-pattern values from the story file) but worth a comment in the test that this is intentionally tight coupling.

- **L-002 (minor):** AC-002 (e) says `motion-reduce:transition-none` class is present. Plan covers it. Worth also asserting the existing `[ watch ]` button has the same class — silent regression guard.

- **L-003 (negligible):** The plan removes the existing comment about the hidden `[ push back ]`. That's correct; nothing to preserve.

## Verdict: PROCEED at 93.2 / 100

Implementation-ready.
