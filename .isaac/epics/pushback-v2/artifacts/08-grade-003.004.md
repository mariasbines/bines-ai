# Self-Grade: 003.004 — `/api/argue-judge/run` route

**Mode:** self-grade (Codex CLI quota exhausted; orchestrator authored).
**Subject:** `07-plan-003.004.md`.
**Pass threshold:** 85 / 100.
**Result:** **92 / 100 — PASS.**

---

## Rubric

### 1. Plan completeness (19 / 20)

All 11 ACs mapped to numbered steps + tests. Auth-model decision tree articulated in §Design 1 with exact status-code mapping. Salt-rotation tolerance pattern from `argue-log` reused. Edge-runtime constraint enumerated.

The plan covers:
- Auth model (no shared secret, distinct error codes — §Design 1).
- Edge runtime confirmation (§Design 2).
- IP extraction reuse from chat route (§Design 3).
- Salt-rotation tolerance with null-guard for absent previous salt (§Design 4).
- Two-day argue-log scan with rationale (§Design 5).
- NaN-defensive recency check (§Design 6).
- Idempotency-after-auth ordering (§Design 7).
- Runner + storage write transactional integrity (§Design 8).
- Response shape with `X-Governed-By` always present (§Design 9).
- Mock pattern for all four library deps (§Design 11).

**Penalty (-1)**: the plan does not enumerate the test fixture for an `ArgueLogEntry` (the body of the entry that the test will inject as the "latest" entry). Easy to fix during execute by reading `validEntry()` from `argue-log/__tests__/storage.test.ts`. Cosmetic.

**Score: 19/20.**

### 2. AC ↔ code traceability (19 / 20)

Verification matrix explicit on all 11 ACs. AC-009's "no 401 anywhere" is mapped to "implicit via test matrix" — defensible since no test asserts a 401 status code, but a sharper "negative" test (`expect(res.status).not.toBe(401)` across a sweep of error paths) would tighten this.

**Penalty (-1)**: AC-007's "no partial verdict written on runner throw" is documented in §Design 8 ("transactional integrity") but the test for it relies on `appendArgueJudge` not being called — a positive `expect(mockAppendArgueJudge).not.toHaveBeenCalled()` post-throw assertion. The plan lists this in the test matrix; could call it out as the explicit AC-007 trace.

**Score: 19/20.**

### 3. Test coverage plan (18 / 20)

Spec count target ~14-16. Each AC has at least one specific test. Edge cases identified: salt-previous unset, salt-previous set with current match, multi-day entry split, runner-throw transactional check, exactly-30-min boundary.

**Penalty (-1)**: the "exactly 30 min boundary" case is mentioned in edge-cases but not separated into "within = ok" vs "over = 410". Two specs would tighten this; the plan implies one spec per side.

**Penalty (-1)**: no explicit test for the route's behaviour when `readArgueLogDay` itself throws (e.g., Blob unreachable). The plan's "fail-shut" implicit assumption is that this surfaces as a 500. Better to have a dedicated spec.

**Score: 18/20.**

### 4. Risk mitigation (19 / 20)

All three risks from `04-assessment.md` mapped to specific plan + test artifacts:
- PB2-SEC-002: six-layer mitigation enumerated (IP-bind, salt rotation, recency, idempotency, distinct codes, full test matrix).
- PB2-SEC-007: covered by PB2-SEC-002's IP-bind — explicit recognition that the two risks share a mitigation surface.
- PB2-OPS-003: idempotency is the dedupe gate; matches loader-side dedupe.

**Penalty (-1)**: PB2-SEC-002 mitigation could include a dedicated "no oracle on which conversation_ids exist" test — currently the spec relies on the test matrix structure to prove it (different codes for different states) but a focused regression spec would belt this.

**Score: 19/20.**

### 5. Scope discipline (17 / 20)

Files list exact match: 2 created (route + tests), 0 modified, 0 deleted. All seven scope-rule items from the story are echoed in §Design 12.

**Penalty (-2)**: the plan introduces a NaN-defensive recency check (§Design 6, "treat unparseable as expired"). This is a judgment call — the AC didn't ask for it, but the alternative (treat NaN as fresh) fails-open. Defensive scope addition; documented in the plan but counts as a small expansion.

**Penalty (-1)**: the plan documents "idempotency check happens AFTER auth (IP-bind + recency)" — this is a design choice not explicitly in the AC list. AC-006 says "Idempotency check"; ordering is implicit. The plan's choice (post-auth) is correct for the no-oracle property but is a derived requirement. Cosmetic on scope.

**Score: 17/20.**

---

## Total: 19 + 19 + 18 + 19 + 17 = **92 / 100. PASS.**

Threshold ≥ 85. Plan proceeds to execute.

---

## Issues to address during execute (not blocking grade)

1. **Add an explicit "no 401 status on any error path" sweep test.** Single spec asserting `expect(res.status).not.toBe(401)` across all error paths.

2. **Add a spec for `readArgueLogDay` throwing (Blob unreachable).** Should bubble as 500.

3. **Split the recency spec** into two: at-30-min-mark (ok) and just-over (410).

4. **Add an explicit test that idempotency happens AFTER auth** — i.e., a request with wrong IP + an existing verdict still returns 403 (NOT a 200 leak of verdict existence).

5. **Validate fixture builder for `ArgueLogEntry`** — copy `validEntry()` from `argue-log/__tests__/storage.test.ts`; extend with `conversation_id` + `from_slug` defaults.

None of these affect the grade. All five tighten coverage.

---

## Confidence

High. The route is a single file with clear control flow. The novel auth model is well-locked by the architecture. The mocking surface is wide (four library modules) but each mock is straightforward — no SDK timing or async intricacies like the runner's AbortController.

Proceeding to execute.
