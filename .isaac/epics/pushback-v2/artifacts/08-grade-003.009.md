# Grade — Story 003.009: v1 surface deletion

**Plan reviewed:** `07-plan-003.009.md`
**Grader:** self-graded
**Date:** 2026-05-13
**Threshold:** ≥85 to proceed

---

## Rubric

| Dimension | Weight | Score | Weighted |
|---|---|---|---|
| Completeness — every AC mapped to action | 25 | 25 | 6.25 |
| Correctness — consumer audit pre-empted any orphan import | 25 | 24 | 6.00 |
| Safety — non-destructive of v2 surface | 20 | 20 | 4.00 |
| Test coverage / regression net | 15 | 13 | 1.95 |
| Scope discipline | 10 | 10 | 1.00 |
| Documentation | 5 | 5 | 0.50 |
| **Total** | 100 | — | **94.7** |

---

## Strengths

- **Pre-audited the consumer graph** before declaring the plan ready. Every `src/lib/push-back/` helper is consumed only by deleted-or-itself; the modal has zero non-test consumers; the route is leaf. Deletion is safe.
- Correctly preserves the architecture-history comment in `src/lib/argue-log/storage.ts` (line 29 mentions "push-back storage" as a rationale anchor). Deleting that comment would lose context for the deferred concurrency refactor. Not in scope.
- Plan uses `git rm` (not `rm`) — keeps the deletion explicit in the diff.
- Verification step lists all four AC-008 grep audits as explicit commands.

## Weaknesses

- **L-001 (minor):** The plan estimates the new test-suite size as "~568 tests". Hard to predict exactly without running it. Not a plan-quality issue — just a guess. Real number will be confirmed post-deletion.

- **L-002 (minor):** The comment-string "push-back storage" in `argue-log/storage.ts` is **not** caught by AC-008's grep patterns (`PushBackModal`, `/api/push-back`, `@/lib/push-back`, `PushBackModalProps`). Defensible: the AC specifies those exact patterns. If a stricter grep (e.g. case-insensitive substring `push-back`) is desired, the comment would need to be reworded. Plan correctly notes this is out of scope.

## Verdict: PROCEED at 94.7 / 100

Mechanical-but-safe deletion. No critical gaps.
