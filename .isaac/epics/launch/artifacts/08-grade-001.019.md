# Plan Grade: 001.019 — CI + automated Claude PR review

**Graded by**: Grade Agent (claude-sonnet-4-6) with Codex CLI verification
**Date**: 2026-04-22T00:00:00Z
**Recommendation**: APPROVED

## Score: 95/100

| Category | Weight | Raw | Weighted |
|----------|--------|-----|----------|
| Completeness | 25% | 97 | 24.25 |
| Testability | 25% | 95 | 23.75 |
| Clarity | 20% | 97 | 19.40 |
| Risk mitigation | 15% | 95 | 14.25 |
| Alignment | 15% | 88 | 13.20 |
| **Total** | | | **94.85 → 95** |

### Finding counts

- Critical: 0
- High: 0
- Medium: 1
- Low: 2

---

## Findings

### Medium

**M-001 — Story references `@octokit/rest`; plan correctly uses `gh` CLI instead, but this deviation is undocumented.**

Story technical task 3 says: "ESM module using `@anthropic-ai/sdk` + `@octokit/rest` (or `gh` CLI via `child_process`)". The plan silently chose `gh` CLI and `@octokit/rest` is not in `package.json` (confirmed via codebase check). The choice is correct and the plan's rationale section mentions it, but the story's preferred primary option (`@octokit/rest`) is never acknowledged as rejected.

Consequence: if a future reviewer reads the story expecting `@octokit/rest` they will be confused. This is a documentation gap, not an execution risk.

**Suggested fix**: add one sentence to the Step 4 rationale: "`@octokit/rest` was evaluated but skipped — `gh` CLI is pre-installed on `ubuntu-latest`, avoids a redundant dep, and covers both `pr diff` and `pr comment` cleanly."

---

### Low

**L-001 — `pnpm/action-setup@v4` `run_install: false` duplicated in both workflows without cross-reference.**

Minor: if the pnpm version is ever bumped, it must be updated in two places (`ci.yml` line 52 and `claude-review.yml` line 118). No impact today, but a consistency risk over time.

No action required before execute. Acceptable for v1; consider pinning via a reusable workflow or matrix in a follow-up story if the workflow count grows.

**L-002 — YAML `if` expression syntax: workflow uses `${{ !contains(...) }}` — technically correct but GitHub docs show bare expression syntax without `${{ }}` for `if:` fields.**

The `if: ${{ !contains(github.event.pull_request.labels.*.name, 'skip-review') }}` form is valid (GitHub accepts both). PyYAML parses clean (verified). No change needed; noting for awareness.

---

## Verification Evidence

### YAML parse check
Both `ci.yml` and `claude-review.yml` were extracted from the plan and parsed via PyYAML — both parse clean with no errors.

### ESM import resolution
`import Anthropic from '@anthropic-ai/sdk'` resolves correctly — `@anthropic-ai/sdk@^0.90.0` is present in `package.json` dependencies. `node:fs/promises` and `node:child_process` are Node built-ins, no install needed.

### `@octokit/rest` absent from `package.json`
Confirmed not in deps or devDeps. Plan correctly uses `gh` CLI instead (pre-installed on GitHub Actions `ubuntu-latest` runners). No issue in execution; only the documentation gap noted in M-001.

### pnpm version alignment
`package.json` `packageManager` field: `pnpm@10.15.0`. Both workflows pin `pnpm/action-setup@v4` with `version: 10.15.0`. Exact match confirmed.

### CLAUDE.md telemetry section safety
Current `CLAUDE.md` ends at the "Non-negotiables for this project" section. Plan's CLAUDE.md modification appends a new "## PR review flow" section at the end of the file. No edits to the existing telemetry-disabled section. Safe.

### `@anthropic-ai/sdk` import verified live
Running `node --input-type=module` with all three imports from the plan (`@anthropic-ai/sdk`, `node:fs/promises`, `node:child_process`) returned `ESM imports valid` — confirmed on the local codebase where `node_modules/@anthropic-ai/sdk` is installed.

### "Maria does" steps verified as dashboard/CLI actions only
- AC-006 (`gh secret set ANTHROPIC_API_KEY`): gh CLI command — cannot be automated in code without the key itself. Correctly in Step 10.
- AC-007 (branch protection): GitHub Settings dashboard only — correctly in Step 10b. Plan correctly does NOT attempt to set this via code.
- AC-010 (dry-run test PR): correctly assigned to Maria + Claude jointly in Step 10c.

---

## AC Coverage Check

| AC | Covered in plan? | Evidence |
|----|-----------------|----------|
| AC-001 | Yes | Step 2 — full `ci.yml` content with exact triggers |
| AC-002 | Yes | Step 3 — full `claude-review.yml` content |
| AC-003 | Yes | Step 4 — `review-pr.mjs` with `extractStoryId()`, diff, story + context loading |
| AC-004 | Yes | Step 4 — `SYSTEM_PROMPT` covers all 5 rubric points explicitly |
| AC-005 | Yes | Step 4 — `BOT_HEADER` constant, output shape hardcoded in system prompt |
| AC-006 | Yes | Step 10a — `gh secret set` command with both CLI + dashboard options |
| AC-007 | Yes | Step 10b — exact branch protection checkboxes listed |
| AC-008 | Yes | Step 5 — full `pull_request_template.md` content |
| AC-009 | Yes | Step 6 — full CLAUDE.md section content |
| AC-010 | Yes | Step 10c — explicit dry-run procedure with `gh pr checks --watch` |

All 10 ACs mapped. All 8 technical tasks covered (tasks 6–8 appropriately assigned to Maria).

---

## Completeness Check

All files from story scope are addressed:
- CREATE `.github/workflows/ci.yml` — Step 2, exact content
- CREATE `.github/workflows/claude-review.yml` — Step 3, exact content
- CREATE `.github/scripts/review-pr.mjs` — Step 4, exact content
- CREATE `.github/pull_request_template.md` — Step 5, exact content
- MODIFY `CLAUDE.md` — Step 6, exact append content

No out-of-scope files touched. Story scope rules honoured.

---

## Risk Mitigation Check

- **Graceful no-op when `ANTHROPIC_API_KEY` absent**: `if (!ANTHROPIC_API_KEY) { process.exit(0) }` — confirmed in plan Step 4. First test PR before Maria sets the secret will not crash.
- **`skip-review` label escape hatch**: `if: ${{ !contains(..., 'skip-review') }}` — confirmed in `claude-review.yml`.
- **Rollback plan**: single-commit story; `git revert <sha>` is explicit in the Rollback section. Dashboard state (secret + branch protection) rollback steps also documented.
- **No telemetry calls**: plan contains zero references to `report-progress.js` or any ISAAC telemetry script. Compliant with project rule.
- **Token/cost cap**: diff bounded at 40k chars, architecture at 8k chars — explicit in the script constants.

---

## Summary

A well-grounded, execution-ready plan. Both YAML files parse clean. The SDK import is confirmed installed. The pnpm version is exact-match between `package.json` and the workflow pins. All 10 ACs are mapped to exact file content — no pseudo-code, no TBDs, no gaps. The graceful no-op, label escape hatch, and rollback section are all present and specific.

The single medium finding (undocumented `@octokit/rest` deviation) is cosmetic and does not block execution. Both low findings are informational only.

The CLAUDE.md modification is append-only and does not touch the telemetry-disabled section.

**Proceed to `isaac:execute-agent` 001.019.**
