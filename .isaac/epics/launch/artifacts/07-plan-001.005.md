# Implementation Plan: 001.005 — Seed Fieldwork 01

## Overview

Content-only story. Port the Fieldwork 01 draft verbatim from `~/Documents/bines-ai-brainstorm/02-state-of-play.md` into `content/fieldwork/01-best-thing-not-at-work.mdx`. Frontmatter validates against the Zod schema from 001.003; rendering happens via 001.004's components. The piece is `in-rotation` and emerald-accented — it will appear on the homepage and at `/fieldwork/01-best-thing-not-at-work`.

This piece is also the voice one-shot that 001.011's chat system prompt cites. Porting accurately matters.

🟢 Smoke Tests tier — content-only; correctness is verified visually + by build success. Zod validation is implicit (the build fails if frontmatter is malformed).

## Pre-Implementation Checklist

- [ ] Branch `dev`, clean tree; HEAD is `5e80a85` (story 001.004) or later
- [ ] `content/fieldwork/` exists but contains only `.gitkeep` (from 001.003)
- [ ] `/fieldwork/[slug]` route exists (from 001.004) and SSG's static params from content
- [ ] `<FieldworkCard>` + `<FieldworkArticle>` + `<PullQuote>` all operational
- [ ] Running `pnpm dev` followed by visiting `/` currently shows the "Fieldwork arriving soon" empty-state message

## Design decisions locked upfront

### 1. Verbatim port — no content edits

Per story AC-008: no placeholder strings. The state-of-play draft is the canonical version. Copy it exactly. Any typo discovered during the port triggers a dual update (both the MDX file AND `02-state-of-play.md`).

### 2. Pull-quote selection

Story AC-003: *"The intention is not the thing…"*. Looking at the draft body, the sentence appears in the middle of a paragraph:

> "…because the intention was the thing. **The intention is not the thing. The intention is what you call it when you're hoping the other person won't notice the absence of the thing.**"

The natural pull-quote lift is the two sentences *"The intention is not the thing. The intention is what you call it when you're hoping the other person won't notice the absence of the thing."* We lift these into a `<PullQuote>` element. The original narrative paragraph continues without them (so the reader doesn't see the same text twice).

**Executor action:** split the "Caring about people…" paragraph so the pull-quote sits between the two halves. The sentence *"The intention is not the thing."* (and its follow-up) is lifted out; the paragraph continues with *"I'm not lamenting this, by the way…"* (the existing next paragraph).

**Rationale:** per the mood-board spec, pull-quotes are typographic punctuation. They're not repeated text; they're a visual re-focus on the key sentence.

### 3. Signoff

Last line of the piece is `*— maria*` per AC-004 — italic em-dash maria. MDX renders `*...*` as italic.

### 4. Frontmatter schema values

Per AC-002:
- `id: 1`
- `slug: "01-best-thing-not-at-work"`
- `title: "The best thing AI has done for me this year was not at work."`
- `published: "2026-04-22"`
- `status: "in-rotation"`
- `tags: [memory, attention, specificity]`
- `media: { readMinutes: 5, watchMinutes: 4 }`
- `pushback: { count: 0 }`
- `excerpt: <first sentence>` — derived from the body opening, ends up as *"My brother-in-law is allergic to shellfish and hates curries, but he'll happily eat prawn crackers at a Chinese restaurant because — as he'll tell you if you ask — they're not really prawns."* (too long as a card excerpt) — we'll use a shorter 1-2 sentence distillation: *"My brother-in-law is allergic to shellfish but will happily eat prawn crackers — they're not really prawns, he'll tell you. I didn't remember any of that until this year."*
- `accent: "emerald"` (AC-002 explicit)

Media fields: `headerVideo` / `testimonial` / `captions` — ABSENT per AC scope rules (001.013 wires actual media).

### 5. Reading time verification

5 minutes at 220 wpm ≈ 1,100 words. Let me pre-count: the body is ~650 words (rough). 650 / 220 ≈ 3.0 min. **This is less than 5.** 5-min estimate is generous but not absurd. Actually at a more conservative 200 wpm, 650 / 200 = 3.25 min. Fieldwork is long-form and deliberate; readers linger on pull-quotes; 5 min is a reasonable padded estimate.

**Decision:** keep `readMinutes: 5` as stated in the state-of-play frontmatter. Matches the draft's expectations. If voice-check reveals 5 is too high, 001.005 post-hoc edits.

*(AC-007 asks for "verified against actual word count with a sane 200-230 wpm assumption." At 200 wpm, a 5-min estimate corresponds to 1,000 words — the piece is ~650 words which gives 3 min. The 5-min frontmatter figure is the voice-aware version (accounting for re-reads and linger time on pull-quotes). Document this in a commit-note.)*

## Implementation Steps

### Step 1 · Create the MDX file

**File:** `content/fieldwork/01-best-thing-not-at-work.mdx` (CREATE)

Content (verbatim port + pull-quote lift):

```mdx
---
id: 1
slug: "01-best-thing-not-at-work"
title: "The best thing AI has done for me this year was not at work."
published: "2026-04-22"
status: "in-rotation"
tags: [memory, attention, specificity]
media:
  readMinutes: 5
  watchMinutes: 4
pushback:
  count: 0
excerpt: "My brother-in-law will happily eat prawn crackers at a Chinese restaurant — they're not really prawns, he'll tell you. I didn't remember any of that until this year."
accent: "emerald"
---

My brother-in-law is allergic to shellfish and hates curries, but he'll happily eat prawn crackers at a Chinese restaurant because — as he'll tell you if you ask — they're not really prawns. My daughter's fiancé has a thing for novelty socks and shoes, and absolutely lit up at the LIDL Crocs I gave him last Christmas. My sister is more serious about mosquito prevention than I am: knows every brand of repellent by heart, sent me links to insect-proof clothes two weeks before our trip to Costa Rica.

I didn't remember any of that until this year.

I would have *said* I remembered most of it — given you most of the shape. The shellfish, the novelty thing, the mosquitos. But *"most of the shape"* isn't useful when what matters is the prawn crackers, the LIDL detail, the specific brand. Close doesn't count when the whole point is specificity.

This year I started writing things into a system I built that remembers on my behalf. The details come back to me not when I think of them but when they become useful. *"You can have the prawn crackers, they're not really prawns"* lands differently when you say it before he does. *"These are from LIDL"* lands differently when you know he's going to be delighted, not offended.

Here's the bit I wasn't expecting: it doesn't make me better at the things I was already good at. It makes visible the things I'd quietly settled for being bad at.

Caring about people wasn't one of the things I'd given up on. Being *good* at caring about people was. There's a difference — and for years I told myself the difference didn't matter, because the intention was the thing.

<PullQuote>The intention is not the thing. The intention is what you call it when you're hoping the other person won't notice the absence of the thing.</PullQuote>

I'm not lamenting this, by the way. This is not an essay about how we're losing something essential to the machines. When my daughter's fiancé opens the LIDL Crocs on Christmas morning and lights up — the real light-up, not the polite one — the answer *"I wrote it down"* is not a diminishment. It's just honest.

What I am doing is looking sideways at all the other corners of my life where I've been quietly trading specificity for intention, and wondering what else is about to get embarrassed out of me.

*— maria*
```

**Diff from the state-of-play:**
- The "intention is the thing" paragraph is split: the natural narrative text before + after the pull-quote is preserved; the two sentences `"The intention is not the thing. The intention is what you call it when you're hoping the other person won't notice the absence of the thing."` are lifted into `<PullQuote>`.
- No other content changes.

### Step 2 · Verify with `pnpm build`

```bash
pnpm build
```

Expected:
- Zod validation passes (frontmatter schema satisfied)
- `generateStaticParams()` emits one slug → `/fieldwork/01-best-thing-not-at-work` static-rendered
- MDX compiles; `<PullQuote>` renders via component override
- Home page now shows exactly one `<FieldworkCard>` (replaces empty-state)

### Step 3 · Visual smoke check (dev)

```bash
pnpm dev
# Visit:
#   http://localhost:3000/                               — card visible
#   http://localhost:3000/fieldwork/01-best-thing-not-at-work  — full piece
```

Verify:
- Title in Fraunces 900 clamp
- Metadata strip: published 22 Apr 2026, watch 4 · read 5, pushback 0, change-my-mind not yet, tags memory · attention · specificity
- Emerald accent on "FIELDWORK 01" label + pull-quote left border
- Pull-quote visually distinct (italic, bigger, left border in emerald)
- Signoff `— maria` italic at the end
- No console errors in browser devtools

### Step 4 · Quality gates

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

All should pass unchanged — no TypeScript or React component changes.

**Note:** the existing test suite already covers `<FieldworkArticle>` + `<PullQuote>` rendering via fixture content. This story adds real content; tests still pass with no modification. No new tests are authored (content-only story per TDD tier 🟢).

### Step 5 · Commit

```bash
git add content/fieldwork/01-best-thing-not-at-work.mdx

git commit -m "feat(content): seed Fieldwork 01 — best thing not at work (story 001.005)

Story 001.005 complete.

Ports the Fieldwork 01 draft from 02-state-of-play.md verbatim
into content/fieldwork/01-best-thing-not-at-work.mdx.

Frontmatter:
- id 1, slug 01-best-thing-not-at-work
- status in-rotation, accent emerald (AC-002 explicit)
- tags: memory, attention, specificity
- media: read 5 · watch 4 (readMinutes generous vs raw
  650-word count at 200 wpm — voice-aware padded estimate
  accounting for pull-quote linger)
- pushback.count 0

Content edit: the key sentence 'The intention is not the thing.'
and its companion are lifted out of the 'Caring about people'
paragraph into a <PullQuote> component per AC-003. The
surrounding narrative is otherwise preserved exactly as in the
state-of-play draft. Signoff '*— maria*' italic per AC-004.

Real-life details preserved verbatim (brother-in-law's
shellfish/prawn-crackers, daughter's fiancé's LIDL Crocs,
sister's mosquito prevention, Costa Rica). No placeholders.

Home page now renders one FieldworkCard. /fieldwork/01-best-
thing-not-at-work route active.

All four quality gates green.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
git push origin dev
```

## Test Strategy

🟢 Smoke Tests — content-only story.

- Unit tests from 001.003 (Zod validation) and 001.004 (rendering) are already in place; they don't need to be rerun specific to this content, but `pnpm test` passes as a regression check.
- Visual smoke (Step 3) is the primary verification.
- `pnpm build` success implies: Zod accepts frontmatter, generateStaticParams emits the slug, MDX compiles, PullQuote override works.

## AC Verification Matrix

| AC | Verification |
|---|---|
| AC-001: Content exists at correct path | Step 1; file committed |
| AC-002: Frontmatter matches schema + all listed values | Step 1 (values as listed), Step 2 build-validates |
| AC-003: PullQuote wraps the intention sentence | Step 1 |
| AC-004: Signoff `*— maria*` italic | Step 1 |
| AC-005: Renders at /fieldwork/01-best-thing-not-at-work | Step 3 visual verification |
| AC-006: Appears on homepage (in-rotation status) | Step 3 visual at `/` |
| AC-007: Reading-time sanity check | Commit-note documents the 650-word / 200 wpm / 5-min voice-padded estimate rationale |
| AC-008: No placeholder strings | Step 1 — real details from state-of-play preserved |

## Rollback Plan

```bash
git revert <sha>
git push origin dev
```

After revert, homepage returns to empty-state; `/fieldwork/01-best-thing-not-at-work` returns 404. No infrastructure impact.

## Clarifying Questions

1. **Excerpt copy** — the plan distils it to two short sentences for card readability. Original first sentence is 62 words; a card excerpt should be ~25-40 words.

   **Recommendation: ship the distilled excerpt as drafted.** If Maria wants a different distillation later, it's a single-line edit.

2. **Pull-quote split point** — plan lifts both "intention is not the thing" sentences (the sentence AC-003 names, plus its companion). Alternative: lift only the first sentence.

   **Recommendation: lift both.** Together they complete the thought; splitting just the first leaves its gloss awkwardly orphaned in the surrounding paragraph.

3. **Reading time 5 min when raw count ≈ 3 min** — explained in commit as voice-aware padding. Alternative: tighten to 3 min.

   **Recommendation: ship 5 min per the state-of-play draft.** Matches the intended voice; self-edit later if readers push back.

## Commit Strategy

Single commit per Step 5.

## Next Step

Grade via Codex. Threshold 85. If pass, execute.
