# Story 001.005: Seed Fieldwork 01

## Description

Port the Fieldwork 01 draft from `~/Documents/bines-ai-brainstorm/02-state-of-play.md` into a real MDX file at `content/fieldwork/01-best-thing-not-at-work.mdx`. The draft is already locked with real details (brother-in-law's prawn crackers, LIDL Crocs, Costa Rica sister). Frontmatter must validate against the Zod schema from 001.003. This also serves as the voice-reference one-shot that the chat's system prompt cites in 001.011 — so the canonical version of this piece needs to be sound before chat goes live.

## Acceptance Criteria

- [ ] AC-001: `content/fieldwork/01-best-thing-not-at-work.mdx` exists with the full body matching the "swapped-to-real-details" draft in `02-state-of-play.md` (not the original placeholder version)
- [ ] AC-002: Frontmatter validates against `FieldworkFrontmatter` schema: `id: 1`, `slug: "01-best-thing-not-at-work"`, `title`, `published: "2026-04-22"`, `status: "in-rotation"`, `tags: ["memory", "attention", "specificity"]`, `media: { readMinutes: 5, watchMinutes: 4 }`, `pushback: { count: 0 }`, `excerpt` derived from first sentence, `accent: "emerald"` (explicit — this piece carries emerald)
- [ ] AC-003: Body uses `<PullQuote>` component for the *"The intention is not the thing..."* pull-quote
- [ ] AC-004: Body ends with `*— maria*` signoff in italic
- [ ] AC-005: Piece renders on `/fieldwork/01-best-thing-not-at-work` without errors
- [ ] AC-006: Piece renders on homepage card (is `in-rotation`)
- [ ] AC-007: Reading time estimate (5 min) verified against actual word count with a sane 200-230 wpm assumption
- [ ] AC-008: No placeholder strings left — all "Dan"/"Kyoto stationer"/"Biscuit the whippet" placeholders are replaced per the locked draft (which uses brother-in-law, LIDL Crocs, sister's mosquito expertise)

## Technical Tasks

1. Read `~/Documents/bines-ai-brainstorm/02-state-of-play.md` — the revised Fieldwork 01 section (the one that ships with real details)
2. Port body into `content/fieldwork/01-best-thing-not-at-work.mdx`
3. Write the frontmatter block
4. Wrap the pull-quote paragraph in `<PullQuote>`
5. Verify `pnpm build` succeeds with this content present
6. Visit `/fieldwork/01-best-thing-not-at-work` in dev server — verify typography, accent colour, data strip

## Files

- **CREATE**: `content/fieldwork/01-best-thing-not-at-work.mdx`
- **MODIFY**: none

## Scope rules

- Do NOT invent new content. Port the locked draft verbatim. Only edit if the port reveals a typo; in that case, also update `02-state-of-play.md` so both stay in sync.
- Do NOT attach a video yet. `media.headerVideo` and `media.testimonial` fields left absent in frontmatter; video frontmatter fields populated in 001.013 if videos exist.
- Do NOT add `changeMyMind` — this is the first piece, no revisions yet.

## Test plan (🟢 Smoke Tests)

- Visual (manual): compare rendered `/fieldwork/01-best-thing-not-at-work` to the mood-board Fieldwork card layout and the text in `02-state-of-play.md`
- Command: `pnpm build` succeeds
- Command: `pnpm lint` passes on the MDX (if configured)
- Zod: frontmatter validates — exercised implicitly by the build

## Dependencies

- **Depends on**: 001.004
- **Blocks**: 001.011 (chat system prompt uses this piece as voice reference)

## TDD Tier

🟢 Smoke Tests — content-only story; rendering correctness is what matters and it's covered by 001.004 tests + visual check.

## Estimated Complexity

Small
