# Story 001.001: Foundation — deps, fonts, design tokens

## Description

Scrap the 8-month-old SynapseDx-flavoured placeholder code. Install project dependencies. Configure the Palette A design tokens and the three fonts (Fraunces, Inter, JetBrains Mono) via `next/font`. Put the colophon stamp in `public/` as the favicon. This is the root of the DAG — every other story depends on it.

## Acceptance Criteria

- [ ] AC-001: `src/app/page.tsx`, `src/app/about/**`, `src/app/components/**` are deleted (SynapseDx/healthcare placeholder content removed wholesale)
- [ ] AC-002: All runtime deps installed without peer-conflict warnings: `@anthropic-ai/sdk`, `@vercel/blob`, `@vercel/analytics`, `@upstash/ratelimit`, `@upstash/redis`, `feed`, `@next/mdx`, `@mdx-js/loader`, `@mdx-js/react`, `gray-matter`, `date-fns`, `remark-gfm`, `zod`
- [ ] AC-003: Dev deps installed: `vitest`, `@vitest/ui`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, `prettier`, `prettier-plugin-tailwindcss`
- [ ] AC-004: Fraunces + Inter + JetBrains Mono load via `next/font/google` in root layout; CLS verified zero via Lighthouse
- [ ] AC-005: `app/globals.css` defines Palette A tokens via Tailwind v4 `@theme`: `--color-paper #F0E6D0`, `--color-paper-2 #FAF3DE`, `--color-ink #1A1814`, `--color-emerald #0F7B5A`, `--color-sapphire #1B3A8C`, `--color-ruby #B0213A`, `--color-topaz #C28F2A`, `--color-amethyst #3F1E4C`
- [ ] AC-006: Scaffolded dark-mode `@media (prefers-color-scheme: dark)` block is removed — site is light-ground only (per Palette A decision)
- [ ] AC-007: `public/stamp.svg` exists (copied from `~/Documents/bines-ai-brainstorm/stamp.svg`) and is referenced as the favicon via `icon` metadata
- [ ] AC-008: `pnpm build` succeeds, `pnpm lint` passes, no TypeScript errors

## Technical Tasks

1. Delete: `src/app/page.tsx`, `src/app/about/`, `src/app/components/`, scaffolded placeholder SVGs in `public/` (file.svg, globe.svg, next.svg, vercel.svg, window.svg)
2. `pnpm add` runtime deps (see AC-002)
3. `pnpm add -D` dev deps (see AC-003)
4. Update `next.config.ts` to enable MDX via `@next/mdx` `withMDX()` wrapper; add `pageExtensions: ['ts', 'tsx', 'md', 'mdx']`
5. Configure three fonts in `src/app/layout.tsx` via `next/font/google`, export as CSS variables (`--font-fraunces`, `--font-inter`, `--font-mono`)
6. Write `src/app/globals.css`: `@import "tailwindcss"`, `@theme { ... }` with Palette A tokens + font-family mappings + semantic tokens (`--color-bg → --color-paper`, `--color-surface → --color-paper-2`, `--color-text → --color-ink`)
7. Copy `~/Documents/bines-ai-brainstorm/stamp.svg` to `public/stamp.svg` (use `cp` in a task; do not re-generate)
8. Add favicon metadata in root layout: `export const metadata: Metadata = { icons: { icon: '/stamp.svg' } }`
9. Create `src/lib/utils/cn.ts` exporting a `cn()` className combiner (clsx + tailwind-merge if desired, or minimal native)
10. Remove default Next README from `README.md`; replace with a minimal bines.ai README (title + one line)

## Files

- **DELETE**: `src/app/page.tsx`, `src/app/about/`, `src/app/components/`, `public/{file,globe,next,vercel,window}.svg`
- **CREATE**: `public/stamp.svg`, `src/lib/utils/cn.ts`
- **MODIFY**: `src/app/layout.tsx` (rewrite), `src/app/globals.css` (rewrite), `next.config.ts`, `package.json`, `README.md`

## Scope rules

- Do NOT build any components in this story beyond the layout shell. Components land in 001.002.
- Do NOT write any MDX loaders or content types. That's 001.003.
- `.ico` favicon is out of scope — modern browsers accept SVG favicons; skip generation.
- Do not touch `.isaac/`, `.gitignore`, or ISAAC principle files.

## Risk mitigations

- **SEC-005 prep**: confirm no `NEXT_PUBLIC_*` env vars introduced. Document the rule in `CLAUDE.md` addendum for future stories.

## Test plan (🟢 Smoke Tests)

- Manual: `pnpm dev` → `localhost:3000` loads the empty shell, fonts visible in page source via next/font classes
- Manual: view computed styles on `<body>` → Palette A tokens resolvable
- Manual: browser tab favicon shows the stamp
- Command: `pnpm typecheck && pnpm lint && pnpm build` all pass

## Dependencies

- **Depends on**: none (root of DAG)
- **Blocks**: 001.002, 001.003

## TDD Tier

🟢 Smoke Tests — configuration-only story; breakage is obvious and low-ambiguity.

## Estimated Complexity

Medium
