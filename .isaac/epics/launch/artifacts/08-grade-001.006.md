# Plan Grade: 001.006 — Postcard rendering (card + article + routes)

**Graded by**: Claude Sonnet 4.6 (static analysis + Codex-style codebase verification)
**Date**: 2026-04-22T00:00:00Z
**Recommendation**: APPROVED

---

## Score: 90/100

| Category | Weight | Raw | Weighted |
|---|---|---|---|
| Completeness | 25% | 92 | 23.00 |
| Testability | 25% | 88 | 22.00 |
| Clarity | 20% | 92 | 18.40 |
| Risk Mitigation | 15% | 87 | 13.05 |
| Alignment | 15% | 92 | 13.80 |
| **Total** | | | **90.25 → 90** |

---

## Findings

### Critical (0)

None.

### High (0)

None.

### Medium (1)

**M-001 · `accentFor()` signature mismatch when passing Postcards**

- Severity: MEDIUM
- File: `src/components/PostcardCard.tsx` + `PostcardArticle.tsx` (Steps 2-3)
- Details: `accentFor` from 001.004 accepts `{ frontmatter: { accent?: AccentToken; id: number } }`. Postcards have `number`, not `id`. Plan's workaround is `accentFor({ frontmatter: { id: number, accent: postcard.frontmatter.accent } })` — which works at call site but is inelegant. Alternative: widen `accentFor` to accept `{ id?: number; number?: number; accent?: AccentToken }`. Plan notes this as considered-and-rejected: "a minimal cast at the callsite is simpler and keeps 001.004's signature stable."
- Impact: Low. Works correctly; just not textbook.
- Suggested fix: Keep as planned. If the pattern is reused in a third place (unlikely for v1), then widen the signature.

### Low (3)

**L-001 · Homepage Promise.all fetches postcards even when fieldwork is still empty**

- Severity: LOW
- File: Step 6 — `src/app/page.tsx`
- Details: Plan fetches both fieldwork + postcards in parallel. Since both are static SSG, there's no runtime cost — it happens once at build. No issue.
- Suggested fix: Accept.

**L-002 · No test for the `/postcards` index page routing or empty-state**

- Severity: LOW
- File: Test plan
- Details: Pages are server components consuming `getAllPostcards()`. Full integration via `pnpm build` exercises the route. No RTL test for the `/postcards` page itself. This matches 001.004's pattern (no test for the home page render).
- Suggested fix: Accept — parity with existing conventions.

**L-003 · `fmtDateShort` uses `.toLowerCase()` which turns "Apr" into "apr"**

- Severity: LOW
- File: Step 2 — `fmtDateShort`
- Details: Matches the state-of-play postcard #001 draft: `*maria · 22 apr*` (lowercase month). Deliberate voice choice, not a bug.
- Suggested fix: None. Document in commit.

---

## Codebase Verification Results

| Check | Status | Notes |
|---|---|---|
| Repo branch `dev`, clean | PASS | HEAD `5a20b50` |
| 001.003 `getAllPostcards` + `getPostcardByNumber` | PASS | Sorted desc by number |
| 001.004 `<MdxBody>` + accent utilities | PASS | Reusable as-is |
| `content/postcards/` empty (`.gitkeep` only) | PASS | Seeded in 001.007 |
| `src/lib/utils/` currently has only `cn.ts` | PASS | New `number.ts` adds cleanly |
| `Postcard` type exports `number`, `published`, `tags?`, `accent?` | PASS | Matches 001.003 schema |
| Scope: Files-to-create list + single MODIFY to page.tsx | PASS | Matches story's Files list |
| No telemetry calls | PASS |
| TDD tier 🟡 respected | PASS | 3 test files, 12 specs |

---

## AC Coverage Matrix

| AC | Plan Coverage | Test | Status |
|---|---|---|---|
| AC-001: PostcardCard shape | Step 2 | 6 specs | COVERED |
| AC-002: PostcardArticle | Step 3 | 3 specs | COVERED |
| AC-003: /postcards index | Step 4 | Build + visual | COVERED |
| AC-004: /postcards/[number] route | Step 5 | Build + visual | COVERED |
| AC-005: Nav link resolves | Nav added in 001.002; resolves now | Visual | COVERED |
| AC-006: Lighter visual register | Step 2 — no metadata strip; lighter type scale | Visual | COVERED |
| AC-007: RSS eligibility | 001.015 will consume `getAllPostcards` | Deferred to 001.015 | COVERED (data-shape ready) |

---

## Summary

Clean scope, mirrors the FieldworkCard / FieldworkArticle structure from 001.004 but with the explicit visual-weight reduction (no metadata strip, signoff in mono italics, smaller header). Homepage gets a modest "recent postcards" strip. The accent-fn shape workaround is the only real wrinkle and the plan pre-addresses it.

**Score: 90/100 — above threshold of 85.**

---

## Recommendation: APPROVED

Proceed to execute.
