# Drafts

Pieces kept in the repo but NOT loaded by the site (the content loaders read
`content/fieldwork/` and `content/postcards/` only). Move a file back into its
home directory to publish it.

- `09-the-meter.mdx` — pulled from rotation 10 Jun 2026 for rework (Maria:
  "needs more work"). Its hero assets stay installed at `public/media/fw09/`
  (see the README there for the prompt and The Accident) and rewire via the
  frontmatter `media` block when the piece returns. Keep id 9 / slug
  `09-the-meter` reserved for it.
